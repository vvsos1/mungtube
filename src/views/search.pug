extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 Searching For #{searchingFor}
    .search__videos
        if videos.length === 0
            h5 No Videos Found
        else
            each video in videos
                +videoBlock(video)
            
